doctype html
html(lang='en')

html
    head
        title SP Bookstore - Home
        
body
    div#main
        br
        br
        h1 SP Bookstore
        div#list
            h2 List of Books:
            div#info
                each book in books
                    ul
                    p #{book.isbn} #{book.name} #{book.authors} #{book.genres} #{book.page_num} #{book.stockquantity} #{book.price} 
                br

